import * as React from "react";
import { PermissionProps } from "../../types";
declare const Permission: ({ permissions, hasAny, has, children, }: React.PropsWithChildren<PermissionProps>) => JSX.Element;
export default Permission;
